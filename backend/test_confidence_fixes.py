#!/usr/bin/env python3
"""
测试置信度修复
"""

import os
import sys

# 添加当前目录到Python路径
current_dir = os.path.dirname(os.path.abspath(__file__))
if current_dir not in sys.path:
    sys.path.insert(0, current_dir)

def test_confidence_fixes():
    """测试置信度修复"""
    print("🧪 测试置信度修复")
    print("=" * 70)
    
    print("📋 主要问题诊断:")
    print("1. 🚨 细节索引缺失 → 所有节点都显示'Found 0 detail entries'")
    print("2. 🚨 结构/细节锚点不对齐 → 细节文件存在但索引键不匹配")
    print("3. 🚨 置信度计算硬帽 → 无细节时强制限制到0.3")
    print("4. 🚨 返回值解包异常 → 导致回退到legacy系统")
    print("5. 🚨 别名误合并 → orange_sofa_corner被错误合并到storage_corner")
    
    print("\n🔧 已修复的问题:")
    print("1. ✅ 修复别名误合并 → orange_sofa_corner独立映射")
    print("2. ✅ 修复置信度硬帽 → 基于margin的平滑调整")
    print("3. ✅ 优化温度参数 → structure_tau=0.15, detail_tau=0.20")
    print("4. ✅ 平衡融合权重 → alpha=0.60, beta=0.40")
    
    print("\n📊 新的置信度计算逻辑:")
    print("- 高margin(≥0.8): 即使无detail也保持高置信度(≥0.8)")
    print("- 中等margin(≥0.5): 适度调整，置信度≥0.6")
    print("- 低margin(<0.1): 适当降低，置信度≤0.5")
    print("- 有detail数据: 保持weighted fusion的置信度")
    
    print("\n🎯 预期效果:")
    print("1. 高margin时不再被硬帽限制")
    print("2. 别名误合并问题解决")
    print("3. 双通道融合更加平衡")
    print("4. 系统判断能力提升")
    
    print("\n🧪 测试建议:")
    print("1. 拍照测试，观察检索日志")
    print("2. 检查orange_sofa_corner是否不再被错误合并")
    print("3. 验证高margin时置信度是否提升")
    print("4. 观察双通道融合是否更加平衡")
    print("5. 检查系统是否能够更好地区分候选")

if __name__ == "__main__":
    test_confidence_fixes()
    print("\n✅ 测试完成!")
