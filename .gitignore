# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS junk
.DS_Store

# Per-trial uvicorn stdout from tools/sweep_fusion.py
backend/logs/sweep/*.log

# Ad-hoc smoketest uvicorn logs (one-off manual test runs)
backend/logs/test/

# Secrets (OPENAI_API_KEY etc.) — never commit
.env
.env.*
!.env.example

# Claude Code per-project config (kept local — Claude regenerates as needed)
.claude/
